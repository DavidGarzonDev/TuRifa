# Node.js (Backend)
node_modules/
package-lock.json
yarn.lock

# Client (React/Vite)
client/node_modules/
client/package-lock.json
client/yarn.lock
client/dist/
client/.env
client/.env.local
client/.env.development.local
client/.env.test.local
client/.env.production.local
client/.eslintcache

# Server Build (Backend)
# If you have a build step for your server, uncomment the following line and adjust the path
# dist/
# build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables (Root Level - Backend)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and OS Files
.idea/
.vscode/
*.swp
*~
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Other
/coverage
/tmp
/.history

# Firebase
firebase-debug.log

# Optional: If you use a specific testing framework, add its output directory
# e.g., for Jest:
# /coverage

# Optional: If you use a bundler other than Vite, add its output directory
# e.g., for Webpack:
# /build

# Optional: If you use a database, add its data directory (if applicable)
# e.g., for SQLite:
# *.sqlite
